Primary Key: ObjectId — the default 24 character (12 byte) primary key generated by MongoDB

Foreign key: Foreign keys link data in one table to the data in another table. A foreign key column in a table points to a column with unique values in another table (often the primary key column) to create a way of cross-referencing the two tables.

product.model.js

    supplier: {
        type: mongoose.Schema.Types.ObjectId,
        ref: 'suppliers'
    }

find().populate({
    path: 'supplier',
    select: ''
})

Encryption - Decryption - Authorization

Hashing - Password

    (Plain text - Hash) - bcrypt